# Hybrid-Netflix-style-recommendation-system
Built a hybrid model which combines collaborative filtering, clustering users based on similarity, and watch sequence history of movies. This is implemented using SVD, KNN, XGBoost, Kmeans, Dbscan, and LSTM models.
